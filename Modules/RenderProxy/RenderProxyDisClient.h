// distributor for render proxy
// gather the information of the render server, and get the command
// from distributor manager.

#include "Distributor.h"

class DisClientForRenderProxy{
	DistributorClient * disClient;

public:

};